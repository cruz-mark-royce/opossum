<h1>Listing Your Surveys</h1>

<table>
  <thead>
    <tr>
      <th>User</th>
      <th>Title</th>
      <th>Description</th>
      <th>Published</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @surveys.each do |survey| %>
      <tr>
        <td><%= survey.user %></td>
        <td><%= survey.title %></td>
        <td><%= survey.description %></td>
        <td><%= survey.published %></td>
        <td><%= link_to 'Take', survey %></td>
        <td><%= link_to 'Edit', edit_survey_path(survey) %></td>
        <td><%= link_to 'Destroy', survey, method: :delete %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
